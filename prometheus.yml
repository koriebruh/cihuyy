global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'matching-engine'
    static_configs:
      - targets: ['matching-engine:8080']
    metrics_path: '/metrics'
